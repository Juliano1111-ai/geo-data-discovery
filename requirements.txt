# geo-data-discovery — Python requirements
# ---------------------------------------------------------------------------
# The core engine (skill/geo-data-discovery/scripts/discovery_orchestrator.py)
# and quickstart.py run on the Python 3.9+ STANDARD LIBRARY ALONE — no install
# needed. The packages below unlock optional features and the Colab notebook.
# ---------------------------------------------------------------------------
jsonschema>=4.0      # validate JSON-LD output against schemas/discovery_output.schema.json
pytest>=7.0          # run the test suite in tests/
obspy>=1.4           # download and read real seismic waveforms (miniSEED) and plot them
pandas>=1.5          # load returned CSV / event catalogues into a DataFrame
matplotlib>=3.5      # plot waveforms and quick maps in notebooks
requests>=2.28       # optional convenience for notebook HTTP calls (urllib also works)
