# ReadTheDocs build for the Jupyter Book in docs/
version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  commands:
    - pip install -r docs/requirements.txt
    - jupyter-book build docs/
    - mkdir -p $READTHEDOCS_OUTPUT/html/
    - cp -r docs/_build/html/* $READTHEDOCS_OUTPUT/html/
